import {

} from './actions'

const DEFAULT_STATE = {

}

const rootReducer = (state = DEFAULT_STATE, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default rootReducer;
